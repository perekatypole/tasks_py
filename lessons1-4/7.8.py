numbers = [1, 2, 3, 0]
sum_numbers = sum(numbers)
count_numbers = len(numbers) - 1 # вычитаем 1, потому что последний элемент последовательности - это 0
print(sum_numbers) # выведет 6
print(count_numbers) # выведет 3