p = int(input("Введите натуральное число: ")) # задаем натуральное число через пользовательский ввод
a = [a1, a2, ..., a10] # заданные числа
sum_float = 0 # инициализируем переменную для суммы вещественных чисел
for i in range(10): # проходим по всем числам в списке
    if isinstance(a[i], float): # проверяем, является ли число вещественным
        sum_float += a[i] # если да, то добавляем его к сумме
print("Сумма вещественных чисел:", sum_float)